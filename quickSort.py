# 
def lowToHight(numbers):
    
    while True:
        is_sorted = True
        node = numbers.begin # comparison pointer
        high = None # node > pivot
        last = numbers.end # pivot
        while node:
            pass
        if is_sorted: break